import { EcommerceFormValidators } from './ecommerce-form-validators';

describe('EcommerceFormValidators', () => {
  it('should create an instance', () => {
    expect(new EcommerceFormValidators()).toBeTruthy();
  });
});
